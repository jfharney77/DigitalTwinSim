{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "watch current lesson",
      "type": "shell",
      "command": "make watch-$(basename ${file} .cu | cut -d_ -f1)",
      "options": { "cwd": "${workspaceFolder}" },
      "isBackground": true,
      "problemMatcher": [],
      "presentation": { "reveal": "always", "panel": "dedicated" }
    }
  ]
}
